<article>
  <% @page["slides"].each do |slide| %>
    <% slide_type = slide['Kind'].parameterize %>
    <section class="<%= slide_type %>">
      <div>
        <%= erb :"slides/_#{slide_type}", locals: { slide: slide } %>
      </div>
    </section>
  <% end %>
</article>
